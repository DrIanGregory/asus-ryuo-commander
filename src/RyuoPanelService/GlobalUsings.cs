// The shared BacklightService/MediaService/etc. were written under the WindowsDesktop SDK,
// whose implicit usings omit System.IO; make it global here so the linked files compile.
global using System.IO;
